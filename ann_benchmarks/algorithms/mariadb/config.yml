float:
  euclidean:
  - base_args: ['@metric']
    constructor: MariaDB
    disabled: false
    docker_tag: ann-benchmarks-mariadb
    module: ann_benchmarks.algorithms.mariadb
    name: mariadb
    run_groups:
      M-24:
        arg_groups: [{M: [8, 16, 32, 48, 64]}]
        args: {}
        query_args: [[10, 20, 30, 40]]
